
{% if conflict %}
    <div>
        Conflict found:
        {{ conflict.name }}{{ conflict.constraint }}
    </div>

    <div>
        <ul>
            {% for name, spec in conflict.constraint.specs %}
                <li>
                    {{ spec }} goes from {{ graph.get(name).group }}
                </li>
            {% endfor %}
        </ul>
    </div>
{% endif %}

<div>
    Graph:
    <ul>
        {% for dep in graph %}
            <li>{{ dep.name }}{{ dep.constraint }}</li>
        {% endfor %}
    </ul>
</div>

<div>
    Tried {{ mutator.mutations }} mutations.
</div>
